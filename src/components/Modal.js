import React, { Component } from 'react';

import '../styles/Form.scss';

export default class Modal extends Component {
  // @TODO: Replace this code with (or wrap) someone else's prebuilt React Bootstrap component for a Modal
  // e.g., https://react-bootstrap.github.io/components.html#modals
  render() {
    return (
    );
  }
}
